# backlog
repositório que eu fiz para poder fazer uma espécie de backlog com typescript

para rodar é simples, apenas clone o projeto e entre na pasta com:

```
git clone https://github.com/athauvs/backlog-roulette.git
cd backlog-roulette
```

instale as dependências e rode o projeto:

```
npm install
npm run dev
```
